# Support

For usage problems, include your operating system, shell/version, Docker version, selected source/export mode, image reference, and exact error output with secrets removed.

For WSL2 problems also include `wsl --version` and `wsl --list --verbose` output.

Do not post credentials, private registry tokens, private filesystem contents, or sensitive RootFS data.
